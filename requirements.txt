# ============================================================
# Phase 1 核心依赖
# ============================================================

# LangChain 生态
langchain>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0

# DeepAgents (Phase 2 正式使用，Phase 1 先装上)
deepagents>=0.1.0

# LLM — DeepSeek (OpenAI 兼容协议)
langchain-openai>=0.3.0

# LLM — Anthropic (备选)
# langchain-anthropic>=0.3.0

# 向量存储 + 数据库（PostgreSQL + pgvector，统一存储业务数据/向量/Checkpoint）
psycopg[binary]>=3.2.0
langgraph-checkpoint-postgres>=3.0.0

# 本地 Embedding（仅 local provider 用，默认 siliconflow API）
sentence-transformers>=3.0.0

# CLI
click>=8.1.0

# 配置管理
pydantic>=2.0.0
pydantic-settings>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0

# 文件监听
watchdog>=4.0.0

# RSS 订阅
feedparser>=6.0.0
trafilatura>=1.12.0

# Markdown 解析
markdown>=3.5.0

# 日志
loguru>=0.7.0

# Token 计数
tiktoken>=0.7.0

# Web API + UI
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.12

# ============================================================
# 开发依赖
# ============================================================
# pytest / pytest-asyncio / ruff
# 通过 pip install -r requirements-dev.txt 安装
